helm --tiller-namespace kikkerdev install --name kikker-db-mariadb stable/mariadb -f custom-config.yaml

helm --tiller-namespace kikkerdev install --name kikkber-db-mongodb stable/mongodb -f custom-mongodb-config.yaml

== disable sidecar injection
spec:
  template:
    metadata:
      annotations:
        sidecar.istio.io/inject: "false"
helm --tiller-namespace kikkerdev install --name kikkber-db-mongo stable/mongodb -f custom-mongodb-config.yaml
helm --tiller-namespace kikkerdev upgrade kikkber-db-mongo stable/mongodb -f custom-mongodb-config-nopassword.yaml		


helm --tiller-namespace kikkerdev upgrade fancy-gnat kikkerscheduledtasks -f clusterip.yaml

helm --tiller-namespace kikkerdev install --name kikker-rabbitmq stable/rabbitmq -f rabbitmq-config.yaml
have to manually edit it after deployment
do kubectl edit sts MARIADBname
sts = statefulset
https://istio.io/docs/setup/kubernetes/sidecar-injection/
Then see here
at policy disabled
spec:
  template:
    metadata:
      annotations:
        sidecar.istio.io/inject: "false"
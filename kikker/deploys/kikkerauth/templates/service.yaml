apiVersion: v1
kind: Service
metadata:
  name: {{ include "kikkerauth.fullname" . }}
  labels:
    app: {{ include "kikkerauth.name" . }}
    chart: {{ include "kikkerauth.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ include "kikkerauth.name" . }}
    release: {{ .Release.Name }}
